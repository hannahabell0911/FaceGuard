package com.yourpackage.api.com.example.kotlin_faceguard.ui.theme.reminders

import androidx.compose.runtime.Composable

@Composable
fun ReminderForm(onSave: (Reminder) -> Unit) {
    // Form fields for reminder details
    // onSave is called with the reminder data when the user saves the form
}
